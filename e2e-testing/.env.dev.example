API_URL=http://localhost:5000
WEB_URL=http://localhost:5173

# Buyer (TC-001 login, buyer flows)
BUYER_EMAIL=buyer@test.com
PASSWORD=Test750!!

# Legacy API tests (optional)
TEST_EMAIL=test@example.com
TEST_PASSWORD=password123

# Seller (seller-setup, seller flows)
SELLER_EMAIL=seller@test.com

# Rate limit overrides — set these on the backend when running rate-limit tests.
# Small values let the test suite trigger 429s quickly without thousands of requests.
# RATE_LIMIT_AUTH_MAX=3
# RATE_LIMIT_API_MAX=5
